{
  "name": "casspr-ai",
  "version": "1.0.0",
  "description": "Casspr - AI-powered Twitter/X engagement assistant Chrome extension",
  "private": true,
  "scripts": {
    "generate-icons": "node scripts/generate-icons.js",
    "build": "npm run generate-icons",
    "postinstall": "npm run generate-icons",
    "test:api": "node scripts/test-api-keys.js",
    "test:extension": "node scripts/test-extension.js"
  },
  "devDependencies": {
    "dotenv": "^16.3.1",
    "sharp": "^0.33.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
